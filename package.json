{
    "private": true,
    "scripts": {
        "build": "yarn workspaces run build",
        "prettier": "prettier --write '**/*.{ts,tsx,js,jsx,css,scss,html}'"
    },
    "workspaces": {
        "packages": [
            "mobile",
            "timer",
            "results",
            "server",
            "shared",
            "panel"
        ]
    },
    "resolutions": {
        "@types/eslint": "8.4.3"
    }
}
